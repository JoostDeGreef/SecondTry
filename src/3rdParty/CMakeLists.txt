# 3rd party libraries    
include(FetchContent)

# Remote code

include(googletest)
include(sqlite3)
include(fmt)
include(glfw)
include(zlib)
include(bzip2)
include(brotli)
include(png)
include(freetype2)

# local code

add_subdirectory ("glad")

